services:
  lwconnect-mqtt:
    build: .
    container_name: lwconnect-mqtt
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
      - ./artifacts:/app/artifacts
    security_opt:
      - no-new-privileges:true
